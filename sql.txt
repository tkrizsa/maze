

CREATE TABLE sections (key text primary key, items text);

CREATE TABLE plains (plainid text primary key, info map<text,text>);

INSERT INTO plains (plainId, info) VALUES ('earth', {'left' : '-2000', 'right' : '2000', 'top' : '-2000', 'bottom':'2000', 'defaultFloor' : 'GrassFloor'});


CREATE TABLE login (loginid text primary key, email text, password text);

CREATE INDEX login_email_ix ON login (email);

CREATE TABLE player (playerid text primary key, loginid text, playername text, avatar map<text,text>);

CREATE INDEX player_loginid_ix ON player (loginid);


